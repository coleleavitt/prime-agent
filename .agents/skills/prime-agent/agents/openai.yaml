interface:
  display_name: "Prime Agent"
  short_description: "Repo-specific patterns and workflows for prime-agent"
  default_prompt: "Use the prime-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true