- Dream-RSI: `/dream experiment --seeds a,b,c`, `dream.experiment(seeds=[...])` and the `dream.experiment` host payload run up to 16 independent seeds sequentially under one run id, one `result.json` per seed (`resultPaths` on the status and in the completion row); the Agents View line shows `seed i/n arm r/rounds` and, on the LLM path, the last completed seed's tokens.
- Dream-RSI child agents now take a per-request thinking level (`RunAgentRequest.thinkingLevel`, applied at child construction) and a visible-answer cap (`RunAgentOptions.maxOutputTokens`, injected as the stream `maxTokens` plus the thinking allowance). `/dream`, the `dream` skill and the host payloads expose `--model`, `--thinking` and `--max-output-tokens`; the defaults are thinking off and caps of 4096 (proposer; 8192 for python-speedup), 4096 (dreamer) and 2048 (guidance), recorded in the experiment arm mode and on the `rlm.run_agent` span.
- `/dream --priming none|diverse` and the CLI `--priming` select a fixed pool-priming set; the `dream` skill and every task list now include `autocorrelation`; the CLI `dream experiment` prints per-arm provenance (agent-generated vs local probes, LLM proposals by outcome), a per-step dreaming audit line and the exact probe-granular headline, `dream show` prints each node's origin, and the CLI takes `--beta3` for the anytime term.
