- Dream-RSI: the replay objective's stop-early credit is now evidence-backed and every adoption is checked online. On the measured pool a candidate's spend on each tree is charged at least the latest probe and round at which the same candidate was still improving on the other measured trees (`ReplayResult.roundsToBest` beside `probesToBest`), and the whole budget when there is no other tree, so a single-tree pool can never reward stopping early; the incumbent is charged its raw recorded spend (a symmetric horizon surcharged its real early stops and let candidates win that lose to its actual numbers); the quality guard holds per tree instead of on the pool mean. Because no frozen-pool rule can tell a pool that happened to saturate early from a task that does, the first redeploy rollout of every adopted policy is a probation: when its best falls below the incumbent's lowest replay best on the pool it won on, the adoption is reverted, the incumbent restored and the policy id `revoked` for the run (a new `CandidateVerdict.reason`). `ObjectiveTerms`, `PoolScore` and `CandidateVerdict` carry `chargedProbes` / `chargedRounds`; `PolicySelection` / `DreamResult` carry `currentMinBest`; the round record, the dreams log (`probation` line), the `dream.redeploy` span (`dream.probation`, `dream.probation_floor`, `dream.reverted`), `dream loop` and the experiment output carry the probation judgement; `DreamLoopResult.probationReverts` counts reverts; the dreams-log step line, `dream improve` and the `dream.dream` span carry `evidenceTrees` / `dream.evidence_trees`; and the dreamer prompt states both rules. The recorded run-3 collapse (one 13-node tree whose best was the first probe; the tree is now a fixture) now loses outright on replay, and the run-2 pool keeps a +0.007109 lever.
