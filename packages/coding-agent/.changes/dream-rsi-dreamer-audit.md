- Changed the Dream-RSI LLM dreamer: its prompt now states the replay objective and selection rule, the replay mechanics, every policy field's replay semantics (including the three fields replay cannot reward and the `batchSize` cap at W), the current policy's per-tree replay on the pool and the earlier steps' verdicts; the child's entries are kept up to M distinct new policies with per-entry drop reasons, identical or duplicate entries are audited rather than re-scored, a shortfall is topped up locally (`dreamer` `llm`/`mixed`/`local`), and a fallback is logged as a `role: dreamer` rejection with its cause on the `dream.llm_dream` span.
- Added the dreams log, candidate verdicts, lever scan, `stoppedEarly`, the post-hoc `finalSelection`, exact round curves and child model/thinking/cap attrs to the in-session (LLM-path) dream loop and to every experiment arm; each arm's run id and log keys now carry `<experimentId>/<arm>`, `result.json` gains the probe-granular headline (`probesToTargetExact`, `callsMultiplierExact`) and a note when `k1 <= initialPolicy.beta`, and `primingPolicies` on an experiment prime every arm's round 1 (shared across arms on the LLM path).
