- Changed the Dream-RSI documentation (`docs/dream-rsi.md`, the dream rows of `docs/observability.md`, `evals/dream/README.md` and the `dream` skill) to describe the shipped replay objective with its break-even numbers and the run-2 cancellation identity, the candidate verdict vocabulary, the lever scan, the `dreams/<runId>.jsonl` log, per-arm run ids, pool priming, the probe-granular headline, the pooling rule (within an arm across rounds only; seeds are replicates), in-session seeds, the child model/thinking/output-cap knobs and the `DREAM_TASK_IDS` source of truth; the span rows now say that on the in-session path only `dream.run` and `dream.experiment` are detached roots while `dream.dream`, `dream.replay`, `dream.candidate`, `dream.redeploy` and the `dream.llm_*` spans are their children, and the skill's architecture link points at the file that exists.
