- Dream-RSI tree nodes now record their provenance: every node line carries `origin` (`root`, `local`, or `llm`), `commitAttempt` sets it from the proposer outcome (default `local`) and on the `dream.attempt` span as `dream.origin`, and a tree written before provenance reads as a root plus local nodes. `ExploreResult.agentGeneratedCount` and `TreeSummary.agentGeneratedCount` count the `llm` nodes.
- Dream experiment results separate agent-generated candidates from probes: each round row and the arm totals carry `agentGeneratedCalls` (with a cumulative series), `localFallbacks`, `llmProposals`, `llmAccepted` and `llmRejected` by reason, all additive to schema 1, so a local stand-in for a rejected child output is no longer counted as the agent's work; the headline multipliers stay on probes.
