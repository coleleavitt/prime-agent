- Added a record of how far the conversation moved between a refinement's proposal and its evaluation (`refine.evidence_drift` and message counts on `refine.plan`), and a stale-evidence tag on rejections made after it moved (`refine.stale_evidence`, `staleEvidence` on `refinement.rejected` and the recorded refinement).
- Changed an automatic, agent-requested or failure-repair refinement rejected on stale evidence to plan once more on the current conversation instead of spending its round (its result carries `replanOf` and `refine.replan_of`); a user `/refine` is only tagged.
- Changed the refinement history shown to the planner and the auto-refine reviewer to mark a stale-evidence rejection as a timing artefact and name the refinement that re-planned it.
- Changed the refinement prompts, the auto-refine review and the rendered continual harness overview to ask for a transient condition such as an open blocker or a pending rename to be recorded only together with how to re-check it, and updated or deleted once it changes.
