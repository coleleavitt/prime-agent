- Added a reproducible read-only public metadata audit of all remote catalog endpoints (committed evidence snapshot with pinned-DNS bounded fetches) and merged it into the catalog: observational `auth.metadata` evidence blocks, informational `setup.readiness`/`setup.requirement` classification, documented per-path `auth.alternatives`, and typed setup-field kinds. Placeholder-only OAuth blockers were cleared with evidence (Airtable, Shopify stay Connect-attemptable) while genuine documented requirements stay hard; ready entries are never downgraded by missing evidence.
